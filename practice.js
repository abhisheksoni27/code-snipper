import defaultMember from "module-name";
const iterator = 1;
for (iterator = 0; iterator < 1; iterator++) {
    iterator++;

}
while (10) { 
    //dance
    awesome  = ()=>{
        code.code();
    }
}
while (10) { 
    //dance
    awesome  = ()=>{
        code.code();
    }
}